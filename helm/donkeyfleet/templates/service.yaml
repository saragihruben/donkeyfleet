apiVersion: v1
kind: Service
metadata:
  name: {{ include "donkeyfleet.fullname" . }}
  labels:
    {{- include "donkeyfleet.labels" . | nindent 4 }}
    app.kubernetes.io/component: app
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "donkeyfleet.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: app
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
